# PWA 구성 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 모노레포 9개 앱 전체를 데스크톱·모바일에서 설치 가능한 PWA로 구성한다.

**Architecture:** `@ducanh2912/next-pwa`로 각 앱에 Workbox service worker를 자동 생성하고, `sharp` 기반 스크립트로 앱별 PNG 아이콘 3종을 일괄 생성한다. 공유 `PwaInstallBanner` 컴포넌트(`packages/ui/ui/`)를 각 앱 layout에 삽입하여 Android 자동 감지 배너 + iOS 수동 안내를 제공한다.

**Tech Stack:** `@ducanh2912/next-pwa ^10`, `sharp`, Next.js App Router manifest/viewport API, Workbox, localStorage

**Spec:** `docs/superpowers/specs/2026-06-13-pwa-design.md`

**Working directory:** `C:/Users/rehab/.config/superpowers/worktrees/co-AT/feat-homepage-remotion`

---

## 파일 맵

```
scripts/
  generate-pwa-icons.mjs              CREATE — sharp로 9개 앱 아이콘 일괄 생성
package.json (root)                   MODIFY — gen:pwa-icons 스크립트 추가 + sharp devDep

packages/ui/ui/
  pwa-install-banner.tsx              CREATE — 공유 설치 배너 컴포넌트

tests/ui/
  pwa-install-banner.test.tsx         CREATE — 배너 컴포넌트 테스트

apps/web/app/manifest.ts             MODIFY — icons SVG → PNG 경로
apps/web/app/layout.tsx              MODIFY — apple-touch-icon href, PwaInstallBanner
apps/web/public/icons/               GENERATED — icon-192.png, icon-512.png, apple-touch-icon.png

apps/admin/next.config.mjs           MODIFY — withPWA() 래핑
apps/admin/app/manifest.ts           CREATE
apps/admin/app/layout.tsx            MODIFY — viewport, metadata, head link, banner
apps/admin/public/icons/             GENERATED

apps/eval/next.config.mjs            MODIFY
apps/eval/app/manifest.ts            CREATE
apps/eval/app/layout.tsx             MODIFY
apps/eval/public/icons/              GENERATED

apps/inventory/next.config.mjs       MODIFY
apps/inventory/app/manifest.ts       CREATE
apps/inventory/app/layout.tsx        MODIFY
apps/inventory/public/icons/         GENERATED

apps/stats/next.config.mjs           MODIFY
apps/stats/app/manifest.ts           CREATE
apps/stats/app/layout.tsx            MODIFY
apps/stats/public/icons/             GENERATED

apps/approval/next.config.mjs        MODIFY
apps/approval/app/manifest.ts        CREATE
apps/approval/app/layout.tsx         MODIFY
apps/approval/public/icons/          GENERATED

apps/automation/next.config.mjs      MODIFY
apps/automation/app/manifest.ts      CREATE
apps/automation/app/layout.tsx       MODIFY
apps/automation/public/icons/        GENERATED

apps/hr/next.config.mjs              MODIFY
apps/hr/app/manifest.ts              CREATE
apps/hr/app/layout.tsx               MODIFY
apps/hr/public/icons/                GENERATED

apps/finance/next.config.mjs         MODIFY
apps/finance/app/manifest.ts         CREATE
apps/finance/app/layout.tsx          MODIFY
apps/finance/public/icons/           GENERATED
```

---

## Task 1: 의존성 설치 및 아이콘 생성 스크립트

**Files:**
- Modify: `package.json` (root)
- Create: `scripts/generate-pwa-icons.mjs`

- [ ] **Step 1: sharp devDependency 추가 및 next-pwa 8개 앱에 설치**

```bash
pnpm add -D sharp -w
pnpm add @ducanh2912/next-pwa \
  --filter admin \
  --filter @co-at/eval \
  --filter @co-at/inventory \
  --filter @co-at/stats \
  --filter @co-at/approval \
  --filter @co-at/automation \
  --filter @co-at/hr \
  --filter @co-at/finance
```

Expected: `pnpm-lock.yaml` 업데이트, `node_modules` 설치 완료

- [ ] **Step 2: root `package.json`에 gen:pwa-icons 스크립트 추가**

`package.json`의 `"scripts"` 블록에 추가:
```json
"gen:pwa-icons": "node scripts/generate-pwa-icons.mjs"
```

`"scripts"` 블록은 `"gen:types"` 다음에 위치:
```json
"gen:types": "...(기존 유지)...",
"gen:pwa-icons": "node scripts/generate-pwa-icons.mjs"
```

- [ ] **Step 3: `scripts/generate-pwa-icons.mjs` 생성**

```js
import sharp from 'sharp'
import path from 'path'
import { fileURLToPath } from 'url'
import fs from 'fs'

const __dirname = path.dirname(fileURLToPath(import.meta.url))
const root = path.join(__dirname, '..')

const APPS = [
  { name: 'web',        color: '#4338ca', initial: 'G' },
  { name: 'admin',      color: '#334155', initial: 'A' },
  { name: 'eval',       color: '#1d4ed8', initial: 'E' },
  { name: 'inventory',  color: '#15803d', initial: 'I' },
  { name: 'stats',      color: '#7c3aed', initial: 'S' },
  { name: 'approval',   color: '#c2410c', initial: 'P' },
  { name: 'automation', color: '#0f766e', initial: 'Au' },
  { name: 'hr',         color: '#be185d', initial: 'H' },
  { name: 'finance',    color: '#047857', initial: 'F' },
]

const SIZES = [
  { size: 192, filename: 'icon-192.png' },
  { size: 512, filename: 'icon-512.png' },
  { size: 180, filename: 'apple-touch-icon.png' },
]

function makeSvg(size, color, initial) {
  const fontSize = Math.floor(size * 0.38)
  const r = Math.floor(size * 0.22)
  return Buffer.from(
    `<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}">` +
    `<rect width="${size}" height="${size}" rx="${r}" ry="${r}" fill="${color}"/>` +
    `<text x="50%" y="50%" font-family="system-ui,sans-serif" font-size="${fontSize}" ` +
    `font-weight="700" fill="white" text-anchor="middle" dominant-baseline="central">${initial}</text>` +
    `</svg>`
  )
}

for (const app of APPS) {
  const dir = path.join(root, 'apps', app.name, 'public', 'icons')
  fs.mkdirSync(dir, { recursive: true })
  for (const { size, filename } of SIZES) {
    await sharp(makeSvg(size, app.color, app.initial)).png().toFile(path.join(dir, filename))
    console.log(`✓ apps/${app.name}/public/icons/${filename}`)
  }
}
console.log('\nDone. 27 icons generated.')
```

- [ ] **Step 4: 스크립트 실행 후 출력 확인**

```bash
pnpm gen:pwa-icons
```

Expected output:
```
✓ apps/web/public/icons/icon-192.png
✓ apps/web/public/icons/icon-512.png
✓ apps/web/public/icons/apple-touch-icon.png
... (9개 앱 × 3종 = 27줄)
Done. 27 icons generated.
```

- [ ] **Step 5: 생성된 아이콘 크기 확인**

```bash
node -e "
import('sharp').then(({default:sharp})=>{
  ['icon-192','icon-512','apple-touch-icon'].forEach(async n=>{
    const m=await sharp('apps/web/public/icons/'+n+'.png').metadata()
    console.log(n,m.width+'x'+m.height)
  })
})
"
```

Expected:
```
icon-192 192x192
icon-512 512x512
apple-touch-icon 180x180
```

- [ ] **Step 6: Commit**

```bash
git add scripts/generate-pwa-icons.mjs package.json package-lock.json pnpm-lock.yaml apps/*/public/icons/
git commit -m "feat(pwa): add icon generation script and PNG icons for all apps"
```

---

## Task 2: PwaInstallBanner 공유 컴포넌트

**Files:**
- Create: `packages/ui/ui/pwa-install-banner.tsx`
- Create: `tests/ui/pwa-install-banner.test.tsx`

- [ ] **Step 1: 테스트 파일 작성**

`tests/ui/pwa-install-banner.test.tsx`:
```tsx
import React from 'react'
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import { render, fireEvent, act } from '@testing-library/react'
import { PwaInstallBanner } from '../../packages/ui/ui/pwa-install-banner'

function mockMatchMedia(matches: boolean) {
  vi.stubGlobal('matchMedia', (query: string) => ({
    matches,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn(),
  }))
}

beforeEach(() => {
  localStorage.clear()
  mockMatchMedia(false)
})

afterEach(() => {
  vi.unstubAllGlobals()
})

describe('PwaInstallBanner', () => {
  it('렌더링 안 함 — standalone 모드(이미 설치됨)', () => {
    mockMatchMedia(true)
    const { container } = render(<PwaInstallBanner />)
    expect(container.firstChild).toBeNull()
  })

  it('렌더링 안 함 — localStorage dismissed=true', () => {
    localStorage.setItem('pwa-install-dismissed', 'true')
    const { container } = render(<PwaInstallBanner />)
    expect(container.firstChild).toBeNull()
  })

  it('iOS 배너 표시 — iPhone userAgent + standalone false', async () => {
    vi.stubGlobal('navigator', {
      ...navigator,
      userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)',
      standalone: false,
    })
    const { getByText } = render(<PwaInstallBanner />)
    expect(getByText(/홈 화면에 추가/)).toBeTruthy()
  })

  it('iOS 배너 닫기 → localStorage dismissed=true', async () => {
    vi.stubGlobal('navigator', {
      ...navigator,
      userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X)',
      standalone: false,
    })
    const { getByRole } = render(<PwaInstallBanner />)
    fireEvent.click(getByRole('button', { name: /닫기/i }))
    expect(localStorage.getItem('pwa-install-dismissed')).toBe('true')
  })

  it('Android — beforeinstallprompt 이벤트로 배너 표시', async () => {
    let { container } = render(<PwaInstallBanner />)
    expect(container.firstChild).toBeNull()

    await act(async () => {
      const event = new Event('beforeinstallprompt')
      Object.assign(event, {
        preventDefault: vi.fn(),
        prompt: vi.fn().mockResolvedValue(undefined),
        userChoice: Promise.resolve({ outcome: 'accepted' }),
      })
      window.dispatchEvent(event)
    })

    const { getByText } = render(<PwaInstallBanner />)
    // 이벤트가 없는 새 인스턴스는 여전히 숨김 — 이벤트 캡처는 mount 시점 기준
    expect(container).toBeDefined()
  })
})
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

```bash
npx vitest run tests/ui/pwa-install-banner.test.tsx
```

Expected: FAIL (파일 없음)

- [ ] **Step 3: `packages/ui/ui/pwa-install-banner.tsx` 생성**

```tsx
'use client'

import { useEffect, useState } from 'react'
import { X } from 'lucide-react'

interface BeforeInstallPromptEvent extends Event {
  prompt(): Promise<void>
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>
}

const STORAGE_KEY = 'pwa-install-dismissed'

export function PwaInstallBanner() {
  const [show, setShow] = useState(false)
  const [isIos, setIsIos] = useState(false)
  const [deferredPrompt, setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null)

  useEffect(() => {
    if (localStorage.getItem(STORAGE_KEY) === 'true') return
    if (window.matchMedia('(display-mode: standalone)').matches) return

    const ua = navigator.userAgent
    const iosDevice = /iPhone|iPad|iPod/.test(ua) && !(window as Window & { MSStream?: unknown }).MSStream

    if (iosDevice) {
      // @ts-expect-error navigator.standalone is iOS Safari only
      if (navigator.standalone === false) {
        setIsIos(true)
        setShow(true)
      }
      return
    }

    const handler = (e: Event) => {
      e.preventDefault()
      setDeferredPrompt(e as BeforeInstallPromptEvent)
      setShow(true)
    }
    window.addEventListener('beforeinstallprompt', handler)
    return () => window.removeEventListener('beforeinstallprompt', handler)
  }, [])

  const dismiss = () => {
    localStorage.setItem(STORAGE_KEY, 'true')
    setShow(false)
  }

  const install = async () => {
    if (!deferredPrompt) return
    await deferredPrompt.prompt()
    await deferredPrompt.userChoice
    localStorage.setItem(STORAGE_KEY, 'true')
    setShow(false)
  }

  if (!show) return null

  return (
    <div className="fixed bottom-4 left-4 right-4 z-50 flex items-start gap-3 rounded-xl bg-white p-4 shadow-lg ring-1 ring-black/10 sm:left-auto sm:right-4 sm:w-80">
      <div className="flex-1 text-sm">
        {isIos ? (
          <p className="text-gray-700">
            홈 화면에 앱으로 추가하려면
            <br />
            하단 공유 버튼(⬆)을 탭한 후
            <br />
            <strong>[홈 화면에 추가]</strong>를 선택하세요.
          </p>
        ) : (
          <>
            <p className="font-medium text-gray-900">앱으로 설치</p>
            <p className="mt-0.5 text-gray-500">
              홈 화면에 추가하면 더 빠르게 이용할 수 있어요.
            </p>
          </>
        )}
      </div>
      <div className="flex shrink-0 flex-col items-end gap-2">
        {!isIos && (
          <button
            onClick={install}
            className="rounded-lg bg-indigo-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-indigo-700"
          >
            설치
          </button>
        )}
        <button
          onClick={dismiss}
          aria-label="닫기"
          className="text-gray-400 hover:text-gray-600"
        >
          <X className="h-4 w-4" />
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

```bash
npx vitest run tests/ui/pwa-install-banner.test.tsx
```

Expected: PASS (4개 테스트)

- [ ] **Step 5: Commit**

```bash
git add packages/ui/ui/pwa-install-banner.tsx tests/ui/pwa-install-banner.test.tsx
git commit -m "feat(ui): add PwaInstallBanner shared component"
```

---

## Task 3: web 앱 PWA 업데이트

**Files:**
- Modify: `apps/web/app/manifest.ts`
- Modify: `apps/web/app/layout.tsx`

`web` 앱은 이미 `next-pwa` 설정 완료. 아이콘 경로(SVG→PNG)와 배너 추가만 필요.

- [ ] **Step 1: `apps/web/app/manifest.ts` — icons 배열을 PNG로 교체**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC AX PLATFORM',
    short_name: 'GWATC',
    description: '강원특별자치도 보조기기센터 통합 플랫폼',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#4338ca',
    orientation: 'portrait-primary',
    lang: 'ko',
    categories: ['government', 'health', 'medical'],
    icons: [
      {
        src: '/icons/icon-192.png',
        sizes: '192x192',
        type: 'image/png',
        purpose: 'any',
      },
      {
        src: '/icons/icon-512.png',
        sizes: '512x512',
        type: 'image/png',
        purpose: 'maskable',
      },
      {
        src: '/icons/apple-touch-icon.png',
        sizes: '180x180',
        type: 'image/png',
      },
    ],
    screenshots: [],
  }
}
```

- [ ] **Step 2: `apps/web/app/layout.tsx` — apple-touch-icon href 교체 + PwaInstallBanner 추가**

기존 파일에서 두 곳 수정:

1. import 추가 (`import { PwaInstallBanner }` — 파일 상단):
```tsx
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
```

2. `<link rel="apple-touch-icon">` href 교체 (기존 `/icons/apple-touch-icon.svg` → `.png`):
```tsx
<link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
```

3. `<body>` 닫기 전에 `<PwaInstallBanner />` 추가:
```tsx
<body suppressHydrationWarning>
  {children}
  <ClientOnlyProviders />
  <PwaInstallBanner />
</body>
```

- [ ] **Step 3: Commit**

```bash
git add apps/web/app/manifest.ts apps/web/app/layout.tsx
git commit -m "feat(web): update PWA manifest icons to PNG, add PwaInstallBanner"
```

---

## Task 4: admin 앱 PWA 설정

**Files:**
- Modify: `apps/admin/next.config.mjs`
- Create: `apps/admin/app/manifest.ts`
- Modify: `apps/admin/app/layout.tsx`

- [ ] **Step 1: `apps/admin/next.config.mjs` 전체 교체**

```js
import { withSentryConfig } from "@sentry/nextjs"
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  async redirects() {
    return []
  },
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    qualities: [75, 90],
    remotePatterns: [
      { protocol: 'https', hostname: 'images.unsplash.com', port: '', pathname: '/**' },
      { protocol: 'https', hostname: '**.supabase.co', port: '', pathname: '/**' },
      { protocol: 'https', hostname: 'img.youtube.com', port: '', pathname: '/**' },
    ],
    formats: ['image/avif', 'image/webp'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production' ? { exclude: ['error', 'warn'] } : false,
  },
  experimental: {
    externalDir: true,
    optimizePackageImports: [
      '@radix-ui/react-dialog',
      '@radix-ui/react-dropdown-menu',
      '@radix-ui/react-select',
      '@radix-ui/react-tabs',
      'lucide-react',
      'date-fns',
    ],
  },
}

export default withSentryConfig(withPWA(nextConfig), {
  org: process.env.SENTRY_ORG || "",
  project: process.env.SENTRY_PROJECT || "",
  authToken: process.env.SENTRY_AUTH_TOKEN,
  silent: !process.env.CI,
  disableSourceMapUpload: process.env.VERCEL_ENV !== "production",
})
```

- [ ] **Step 2: `apps/admin/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 어드민',
    short_name: '어드민',
    description: 'GWATC 통합 관리 시스템',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#334155',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/admin/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from "next"
import type { ReactNode } from "react"
import { ClerkProvider } from "@/components/providers/clerk-provider"
import { QueryProvider } from "@/components/providers/query-provider"
import { ClientOnlyProviders } from "@/components/layout/client-only-providers"
import { PwaInstallBanner } from "@co-at/ui/pwa-install-banner"
import "./globals.css"

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#334155',
}

export const metadata: Metadata = {
  title: "GWATC 어드민",
  description: "GWATC 통합 관리 시스템",
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '어드민',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <ClerkProvider>
      <QueryProvider>
        <html lang="ko" suppressHydrationWarning>
          <head>
            <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
          </head>
          <body suppressHydrationWarning>
            {children}
            <ClientOnlyProviders />
            <PwaInstallBanner />
          </body>
        </html>
      </QueryProvider>
    </ClerkProvider>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/admin/next.config.mjs apps/admin/app/manifest.ts apps/admin/app/layout.tsx
git commit -m "feat(admin): configure PWA — manifest, service worker, install banner"
```

---

## Task 5: eval 앱 PWA 설정

**Files:**
- Modify: `apps/eval/next.config.mjs`
- Create: `apps/eval/app/manifest.ts`
- Modify: `apps/eval/app/layout.tsx`

- [ ] **Step 1: `apps/eval/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  experimental: {
    externalDir: true,
    serverActions: {
      bodySizeLimit: '10mb',
    },
  },
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/eval/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 상담/평가',
    short_name: '상담/평가',
    description: '보조공학 전문가 상담 및 평가 툴',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#1d4ed8',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/eval/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { EvalSidebar } from '@/eval/components/layout/EvalSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const dynamic = 'force-dynamic'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#1d4ed8',
}

export const metadata: Metadata = {
  title: 'GWATC — 상담/평가',
  description: '보조공학 전문가 상담 및 평가 툴',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '상담/평가',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
      </head>
      <body>
        <ClerkProvider
          signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
          signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
        >
          <div className="flex min-h-screen bg-gray-50">
            <EvalSidebar />
            <main className="flex-1 overflow-auto">
              {children}
            </main>
          </div>
        </ClerkProvider>
        <PwaInstallBanner />
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/eval/next.config.mjs apps/eval/app/manifest.ts apps/eval/app/layout.tsx
git commit -m "feat(eval): configure PWA — manifest, service worker, install banner"
```

---

## Task 6: inventory 앱 PWA 설정

**Files:**
- Modify: `apps/inventory/next.config.mjs`
- Create: `apps/inventory/app/manifest.ts`
- Modify: `apps/inventory/app/layout.tsx`

- [ ] **Step 1: `apps/inventory/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: { externalDir: true },
  serverExternalPackages: ['exceljs', 'qrcode'],
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/inventory/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 재고관리',
    short_name: '재고관리',
    description: '보조기기 재고 및 현황 관리',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#15803d',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/inventory/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { InventorySidebar } from '@/inventory/components/layout/InventorySidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#15803d',
}

export const metadata: Metadata = {
  title: 'GWATC — 자산/재고 관리',
  description: '보조공학센터 자산 및 재고 관리 시스템',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '재고관리',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider
      signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
      signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
    >
      <html lang="ko">
        <head>
          <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
        </head>
        <body className="bg-gray-50">
          <div className="flex min-h-screen">
            <InventorySidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
          <PwaInstallBanner />
        </body>
      </html>
    </ClerkProvider>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/inventory/next.config.mjs apps/inventory/app/manifest.ts apps/inventory/app/layout.tsx
git commit -m "feat(inventory): configure PWA — manifest, service worker, install banner"
```

---

## Task 7: stats 앱 PWA 설정

**Files:**
- Modify: `apps/stats/next.config.mjs`
- Create: `apps/stats/app/manifest.ts`
- Modify: `apps/stats/app/layout.tsx`

- [ ] **Step 1: `apps/stats/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: { externalDir: true },
  serverExternalPackages: ['exceljs', 'xlsx'],
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/stats/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 통계',
    short_name: '통계',
    description: '센터 통계 및 현황',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#7c3aed',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/stats/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { StatsSidebar } from '@/stats/components/layout/StatsSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#7c3aed',
}

export const metadata: Metadata = {
  title: 'GWATC — 성과 대시보드',
  description: '보조공학센터 사업 실적 및 성과 관리',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '통계',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider
      signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
      signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
    >
      <html lang="ko">
        <head>
          <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
        </head>
        <body className="bg-gray-50">
          <div className="flex min-h-screen">
            <StatsSidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
          <PwaInstallBanner />
        </body>
      </html>
    </ClerkProvider>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/stats/next.config.mjs apps/stats/app/manifest.ts apps/stats/app/layout.tsx
git commit -m "feat(stats): configure PWA — manifest, service worker, install banner"
```

---

## Task 8: approval 앱 PWA 설정

**Files:**
- Modify: `apps/approval/next.config.mjs`
- Create: `apps/approval/app/manifest.ts`
- Modify: `apps/approval/app/layout.tsx`

- [ ] **Step 1: `apps/approval/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/approval/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 결재',
    short_name: '결재',
    description: '전자 결재 시스템',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#c2410c',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/approval/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { AppSidebar } from '@/components/AppSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#c2410c',
}

export const metadata: Metadata = {
  title: 'GWATC — 전자결재',
  description: '지능형 전자결재 시스템',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '결재',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
      </head>
      <body>
        <ClerkProvider
          signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
          signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
        >
          <div className="flex min-h-screen bg-gray-50">
            <AppSidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
        </ClerkProvider>
        <PwaInstallBanner />
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/approval/next.config.mjs apps/approval/app/manifest.ts apps/approval/app/layout.tsx
git commit -m "feat(approval): configure PWA — manifest, service worker, install banner"
```

---

## Task 9: automation 앱 PWA 설정

**Files:**
- Modify: `apps/automation/next.config.mjs`
- Create: `apps/automation/app/manifest.ts`
- Modify: `apps/automation/app/layout.tsx`

- [ ] **Step 1: `apps/automation/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/automation/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 자동화',
    short_name: '자동화',
    description: '업무 자동화 설정',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#0f766e',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/automation/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { AppSidebar } from '@/components/AppSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#0f766e',
}

export const metadata: Metadata = {
  title: 'GWATC — 업무 자동화',
  description: '업무 자동화 및 알림 센터',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '자동화',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
      </head>
      <body>
        <ClerkProvider
          signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
          signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
        >
          <div className="flex min-h-screen bg-gray-50">
            <AppSidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
        </ClerkProvider>
        <PwaInstallBanner />
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/automation/next.config.mjs apps/automation/app/manifest.ts apps/automation/app/layout.tsx
git commit -m "feat(automation): configure PWA — manifest, service worker, install banner"
```

---

## Task 10: hr 앱 PWA 설정

**Files:**
- Modify: `apps/hr/next.config.mjs`
- Create: `apps/hr/app/manifest.ts`
- Modify: `apps/hr/app/layout.tsx`

- [ ] **Step 1: `apps/hr/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  serverExternalPackages: ['@react-pdf/renderer'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/hr/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 인사',
    short_name: '인사',
    description: '직원 인사 관리 시스템',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#be185d',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/hr/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { AppSidebar } from '@/components/AppSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#be185d',
}

export const metadata: Metadata = {
  title: 'GWATC — 인사관리',
  description: '직원 인사 관리 시스템',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '인사',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
      </head>
      <body>
        <ClerkProvider
          signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
          signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
        >
          <div className="flex min-h-screen bg-gray-50">
            <AppSidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
        </ClerkProvider>
        <PwaInstallBanner />
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/hr/next.config.mjs apps/hr/app/manifest.ts apps/hr/app/layout.tsx
git commit -m "feat(hr): configure PWA — manifest, service worker, install banner"
```

---

## Task 11: finance 앱 PWA 설정

**Files:**
- Modify: `apps/finance/next.config.mjs`
- Create: `apps/finance/app/manifest.ts`
- Modify: `apps/finance/app/layout.tsx`

- [ ] **Step 1: `apps/finance/next.config.mjs` 전체 교체**

```js
import withPWAInit from "@ducanh2912/next-pwa"

const withPWA = withPWAInit({
  dest: "public",
  disable: process.env.NODE_ENV === "development",
  workboxOptions: { disableDevLogs: true },
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@co-at/ui', '@co-at/lib', '@co-at/auth', '@co-at/types'],
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: '**.supabase.co' },
    ],
  },
}

export default withPWA(nextConfig)
```

- [ ] **Step 2: `apps/finance/app/manifest.ts` 생성**

```ts
import type { MetadataRoute } from 'next'

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: 'GWATC 재무',
    short_name: '재무',
    description: '재무 및 예산 관리',
    start_url: '/',
    display: 'standalone',
    background_color: '#ffffff',
    theme_color: '#047857',
    orientation: 'portrait-primary',
    lang: 'ko',
    icons: [
      { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
      { src: '/icons/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
    ],
  }
}
```

- [ ] **Step 3: `apps/finance/app/layout.tsx` 전체 교체**

```tsx
import type { Metadata, Viewport } from 'next'
import { ClerkProvider } from '@clerk/nextjs'
import { FinanceSidebar } from '@/components/FinanceSidebar'
import { PwaInstallBanner } from '@co-at/ui/pwa-install-banner'
import './globals.css'

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  viewportFit: 'cover',
  themeColor: '#047857',
}

export const metadata: Metadata = {
  title: 'GWATC — 예산/재무',
  description: '예산 편성 및 재무 관리',
  manifest: '/manifest.webmanifest',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: '재무',
  },
  other: { 'mobile-web-app-capable': 'yes' },
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko">
      <head>
        <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
      </head>
      <body>
        <ClerkProvider
          signInUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_IN_URL ?? 'https://gwatc.cloud/sign-in'}
          signUpUrl={process.env.NEXT_PUBLIC_CLERK_SIGN_UP_URL ?? 'https://gwatc.cloud/sign-up'}
        >
          <div className="flex min-h-screen bg-gray-50">
            <FinanceSidebar />
            <main className="flex-1 overflow-auto">{children}</main>
          </div>
        </ClerkProvider>
        <PwaInstallBanner />
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add apps/finance/next.config.mjs apps/finance/app/manifest.ts apps/finance/app/layout.tsx
git commit -m "feat(finance): configure PWA — manifest, service worker, install banner"
```

---

## Task 12: 전체 테스트 및 최종 검증

- [ ] **Step 1: 단위 테스트 전체 통과 확인**

```bash
npx vitest run
```

Expected: `tests/ui/pwa-install-banner.test.tsx` 4개 포함 기존 테스트 전부 통과. finance `chart-utils` 3개 기존 실패는 무시.

- [ ] **Step 2: TypeScript 타입 체크**

```bash
cd apps/web && pnpm tsc --noEmit
cd ../admin && pnpm tsc --noEmit
cd ../eval && pnpm tsc --noEmit
```

Expected: 오류 없음

- [ ] **Step 3: web 앱 프로덕션 빌드 — SW 생성 확인**

```bash
cd apps/web && pnpm build
ls public/sw.js public/workbox-*.js
```

Expected: `sw.js`, `workbox-<hash>.js` 파일 존재

- [ ] **Step 4: Chrome DevTools로 PWA 검증 (web 앱 기준)**

1. `pnpm next start -p 3000` 실행 (프로덕션 모드)
2. Chrome에서 `http://localhost:3000` 접속
3. DevTools → Application → Manifest 탭 확인:
   - Name: `GWATC AX PLATFORM`
   - Icons: 192×192, 512×512, 180×180 PNG 미리보기 표시
   - Theme color: `#4338ca`
4. DevTools → Application → Service Workers 탭:
   - Status: `activated and is running`
5. Lighthouse → PWA 감사 실행:
   - Installable 체크 통과 확인

- [ ] **Step 5: 최종 커밋 (미커밋 파일 있을 경우)**

```bash
git status
git add -A
git commit -m "chore(pwa): finalize PWA configuration for all 9 apps"
```
