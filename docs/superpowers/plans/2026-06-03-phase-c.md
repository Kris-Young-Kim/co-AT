# Phase C — 1차 앱 마무리 + 안정화

**날짜**: 2026-06-03  
**목표**: 1차 앱(web·admin·eval·inventory·stats) 안정화 및 잔여 기능 완성

---

## 범위

### C-1: GitHub Actions Node.js 24 opt-in (긴급 — 6/16 데드라인)
- `.github/workflows/deploy.yml` job 레벨에 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'` 추가
- `actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4` 모두 대상
- 6월 16일부터 강제 전환 — 미리 처리해서 깨지는 상황 방지

### C-2: CI build filter에 inventory 추가
- `deploy.yml` build step: `--filter=@co-at/inventory...` 누락 상태
- 빌드 검증 없이 배포만 되고 있음 — type error 조기 발견을 위해 추가

### C-3: inventory 재고 부족 알림 Cron
- `apps/inventory/app/api/cron/stock-alert/route.ts` 생성
- 기준: 대여 중 기기가 전체의 90% 초과 시 경고
- `apps/inventory/vercel.json` cron 설정 (매일 09:00 KST = 00:00 UTC)
- 알림은 `inventory_maintenance_logs` 또는 별도 알림 테이블 활용

### C-4: Sentry DSN 환경변수 설정 안내 (수동)
- 코드는 이미 준비됨 — Vercel 각 프로젝트에 `SENTRY_DSN` 추가만 필요
- 사용자가 직접 Vercel 대시보드에서 설정

---

## 순서

1. C-1 (GitHub Actions) — 코드 변경 1줄, 즉시
2. C-2 (CI filter) — 코드 변경 1줄, 즉시
3. C-3 (재고 알림) — 개발 작업, 30분
4. C-4 (Sentry) — 안내만, 즉시

## 완료 기준
- deploy.yml: Node.js 24 경고 없음, inventory build 포함
- inventory Cron: `/api/cron/stock-alert` 응답 200
- TODO.md 모든 항목 ✅
